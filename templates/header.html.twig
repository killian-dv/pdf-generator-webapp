<nav class="flex justify-between p-4 border-b-2 border-slate-200">
    <ul class="flex justify-between gap-4">
        <li><a href="{{ path('app_home') }}">Accueil</a></li>
        <li><a href="{{ path('app_generate_pdf') }}">Générer PDF</a></li>
        <li><a href="{{ path('app_history') }}">Historique</a></li>
        <li><a href="{{ path('app_subscription') }}">Abonnement</a></li>
    </ul>
    <ul class="flex justify-between gap-4">
        {% if app.user %}
            <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
        {% else %}
            <li><a href="{{ path('app_register') }}">Inscription</a></li>
            <li><a class="p-2 bg-blue-600 rounded text-white" href="{{ path('app_login') }}">Connexion</a></li>
        {% endif %}
    </ul>
</nav>